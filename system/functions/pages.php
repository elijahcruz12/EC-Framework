<?php
/**
 * Fixes the Page Title Variable so it looks better.
 */
 $pagetitle = str_replace("-"," ",$pagetitle); //Changes the dashes to spaces so it doesn't show hello-world but instead hello world.
 
 