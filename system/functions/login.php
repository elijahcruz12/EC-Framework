<?php
/**
 * Login feature
 * This helps to not recreate a login everytime you need one.
 */
 
 
 
 function login($array) {
     global $conn, $config;
     
     if(isset($array['username'])) {
         //If we have username
         
     }
     else return 0;
 }